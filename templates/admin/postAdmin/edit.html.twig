{% extends 'admin/base.html.twig' %}

{% block title %}Modification de l'article {{post.id}}{% endblock %}

{% block body %}
    <div class="container">
        <h2>Modification de l'article :  <strong>"{{post.title}}"</strong> </h2>

        {{form_start(form)}}

        {{form_widget(form)}}
        
        {{form_end(form)}}
    </div>

{% endblock %}
