<div class="container">
  {# Titre & logo #}
    <div class="row">
        <div class="col"></div>
        <div class="col">
            <img src="https://via.placeholder.com/200x200" alt="La_ou_mon_coeur">
        </div>
        <div class="col"></div>
    </div>
    
    <nav class="navbar navbar-expand-lg navbar-warning bg-default border border-right-0 border border-left-0 my-5 ">
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarsExample10" aria-controls="navbarsExample10" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>

    <div class="collapse navbar-collapse ml-5 justify-content-between" id="navbarsExample10">
      <ul class="navbar-nav ">
          <li class="nav-item active">
                <a class="nav-link pr-5" href="#">
                    <i class="fas fa-chart-line"></i>
                    Dashboard <span class="sr-only">(current)</span>  
                </a>
          </li>
          <li class="nav-item ">
                <a class="nav-link pr-5" href="{{path('admin_post_index')}}">
                    <i class="Fas fa-newspaper"></i>
                    Blog
                </a>
          </li>
          <li class="nav-item  ">
                <a class="nav-link pr-5" href="{{path('admin_user_index')}}">
                    <i class="fas fa-users"></i>
                    Utilisateurs 
                </a>
          </li>
          <li class="nav-item  ">
                <a class="nav-link pr-5" href="{{path('admin_role_index')}}">
                    <i class="fas fa-comments"></i>
                    Rôles
                </a>
          </li>
          <li class="nav-item  ">
                <a class="nav-link pr-5" href="{{path('admin_comment_index')}}">
                    <i class="fas fa-comments"></i>
                    Commentaires 
                </a>
          </li>
          
          
          

          <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="{{path('admin_account_logout')}}" id="dropdown10" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Deconnexion</a>
              <div class="dropdown-menu" aria-labelledby="dropdown10">
              {# gerer l'affichage si on est connecté ou deconnecté #}
                
                      <a class="dropdown-item " href="{{path('homepage')}}" tabindex="-1" >
                        <i class="fas fa-eye">Voir le site </i>

                      </a>
                    {#<a class="nav-link disabled" href="#" tabindex="-1" aria-disabled="true">Disabled</a>#}  
              </div>
          </li>
          
      </ul>

          
    </div>
  </nav>
  
</div>
{# gerer les annonces sur addflash #}
        {% for label, messages in app.flashes %}
            <div class="container my-3">
                <div class="alert alert-{{label}}">
                {% for message in messages %}
                    <p>{{message| raw}}</p>
                {% endfor %}
                </div>
            </div>
        {% endfor %}
