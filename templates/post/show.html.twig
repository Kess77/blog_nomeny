{% extends 'base.html.twig' %}

{% block title %}{{post.title}}{% endblock %}

{% block body %}
{# Affichage des articles #}
<div class="container"> 
        <div class="row ">
            <div class="col-md-8 my-3">
                <img src="{{ post.coverImage }}" alt="la_mon_ou_mon_coeur" class="rounded mx-auto d-block img-thumbnail" >
                 {% include "post/_titlePost.html.twig" %}

                   {# Mise en place de carroussel #}
                <div id="carouselExampleControls" class="carousel slide" data-ride="carousel">
                    <div class="carousel-inner">
                        {% for image in post.images %}
                            <div class="carousel-item {% if loop.first %} active {% endif %}">
                                <img src="{{image.url}}" class="d-block w-100" alt="...">
                            </div>
                        {% endfor %}    
                </div>
                     <a class="carousel-control-prev" href="#carouselExampleControls" role="button" data-slide="prev">
                        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                        <span class="sr-only">Previous</span>
                     </a>
                     <a class="carousel-control-next" href="#carouselExampleControls" role="button" data-slide="next">
                        <span class="carousel-control-next-icon" aria-hidden="true"></span>
                         <span class="sr-only">Next</span>
                    </a>
                </div>          
            </div>
            {# Affichages des blocks à propos #}
            <div class="col-md-4 my-2">
                <img src="https://via.placeholder.com/400x200"class="img-fluid" alt="la_mon_ou_mon_coeur">
            </div>    
            {# Mis en place des commentaires#}
                {% if post.comments | length > 0 %}
                    <h3 class="mb-3">commentaires</h3>
                        {% for comment in post.comments %}
                            <div class="bg-light rounded mb-3 py-3 px-3">
                                <strong>{{comment.author.fullName}}</strong>a dit:
                                <blockquote>{{comment.content}}</blockquote>
                            </div>
                        {% endfor %}
                {% else %}
                    <h3>Pas de commentaires</h3>
                {% endif %}
               
        </div>
         
</div>
{% endblock %}        