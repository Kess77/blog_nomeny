{% extends 'base.html.twig' %}

{% block title %}la page:{% endblock %}

{% block body %}
{# Affichage des articles #}
<div class="container"> 
        <div class="row ">
            <div class="col-md-8 my-3">
                <img src="{{ post.coverImage }}" alt="la_mon_ou_mon_coeur" >
                <h3 class="h5">Deco</h3>
                 <h2>{{post.title}}</h2>
                <p> {{post.createdAt| date('d-m-Y')}} -  <em>commentaires</em></p>
                <p>{{post.article | raw }}</p>
                
            </div>
             
        </div>
{% endblock %}        