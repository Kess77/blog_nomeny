 {% extends "base.html.twig" %}
 {% block title %}Homepage{% endblock %}
 {% block body %}
    <div class="container">
        <div class="row flex">
      
        {% for post in posts %}
        
            
            <div class="col-8 my-2">
                <img src="{{post.coverImage}}"class="rounded mx-auto d-block img-thumbnail" alt="{{post.slug}}">
                 {% include "post/_titlePost.html.twig" %} 
            </div>
        {% endfor %}
           
            <div class="col-4 my-2">
                <img src="https://via.placeholder.com/400x200"class="img-fluid" alt="">

            </div>
        </div>
    </div>
 {% endblock %}